# Linux packaging gate

Linux remains a developer preview until a distribution/glibc compatibility matrix is measured. The candidate is a native x86_64 onedir tarball; public support must not be claimed merely because it builds on one runner.

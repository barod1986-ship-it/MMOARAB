        ("- إجمالي التعديلات المسجلة: **62,435**", "- إجمالي التعديلات المسجلة: **62,512**"),
        ("- ملفات NPC المراجعة بعمق: **261 من 555**", "- ملفات NPC المراجعة بعمق: **262 من 555**"),
    ]:
        if old not in md:
            raise AssertionError(f"Missing tracker marker: {old}")
        md = md.replace(old, new, 1)

    row261 = "| 261 | الأنظمة والخدمات | بطاقة النادي الرياضي وتدريب زيادة سعة حمل العناصر واستعادة أثر التدريب | `rathena-master/npc/other/gympass.txt` | تحقق دون تعديل: navi_npc_krpri.lub وskillinfolist.lub والمرجع الرسمي | 77 |"
    if row261 not in md:
        lines = md.splitlines()
        pos = max(i for i, line in enumerate(lines) if line.startswith("| 260 |"))
        lines.insert(pos + 1, row261)
        md = "\n".join(lines)

    stage_section = f"""
## المرحلة 261 — مكتملة

- الملف الأساسي: `{MAIN_REL}`
- المرجع الإنجليزي الداخلي مطابق بايتيا للنسخة الرسمية الحالية؛ بصمة Git blob: `{OFFICIAL_BLOB}`.
- روجع حوار كابوس مفتول العضلات كاملا، بما يشمل شرح بطاقة النادي الرياضي، التدريب حتى عشر مرات، نقص البطاقة، بلوغ الحد الأقصى، رفض التدريب، واستعادة المهارة عبر ذاكرة العضلات.
- نُقحت الصياغة العربية وأزيلت المخاطبة المذكرة والعبارات العامية، ووُحد اسم المهارة مع المصطلح المعتمد «زيادة حد الوزن».
- تحقق اسم الشخصية في `navi_npc_krpri.lub` واسم المهارة في `skillinfolist.lub` دون الحاجة إلى تعديل الملفات المرتبطة.
- حُفظ الاسم الداخلي `Ripped Cabus#GymPass` والمعرف `7776` والمتغير `gympassmemory` والمهارة `ALL_INCCARRY` وحد التدريب 10 وجميع المؤثرات والشروط وأوامر الاستهلاك والمنح.
- إجمالي تعديلات المرحلة: **{STAGE_CHANGES}**
- الإجمالي التراكمي: **{CUMULATIVE_CHANGES:,}**
- ملفات NPC المراجعة بعمق: **{NPC_REVIEWED} من {NPC_TOTAL}**
- الملفات المتبقية للمراجعة: **{REMAINING}**
- ملفات المهمات المكتملة: **88 من 88**
- أول ملف متبق: `{NEXT_FILE}`
- التحقق الفني الساكن: **PASS**
""".strip()
    if "## المرحلة 261 — مكتملة" not in md:
        md = md.rstrip() + "\n\n" + stage_section + "\n"
    write_text_preserve(track_md_path, md, md_bom, md_nl)

    # Update general stage CSV.
    track_csv, tc_bom, tc_nl = read_text_preserve(track_csv_path)
    if not re.search(r"(?m)^261,", track_csv):
        track_csv = track_csv.rstrip("\r\n") + "\n" + csv_line([
            261,
            "الأنظمة والخدمات",
            "بطاقة النادي الرياضي وتدريب زيادة سعة حمل العناصر واستعادة أثر التدريب",
            MAIN_REL,
            "تحقق دون تعديل: navi_npc_krpri.lub وskillinfolist.lub والمرجع الرسمي",
            "مكتمل",
            STAGE_CHANGES,
            "المرحلة_261",
        ]) + "\n"
    write_text_preserve(track_csv_path, track_csv, tc_bom, tc_nl)

    # Completed list.
    completed, c_bom, c_nl = read_text_preserve(completed_path)
    if MAIN_REL not in completed:
        completed = completed.rstrip("\r\n") + "\n" + csv_line([
            MAIN_REL,
            "other",
            "مراجعة حوارية وتقنية عميقة",
            "مكتمل",
            261,
            (
                "مراجعة كاملة لكابوس مفتول العضلات في بايون وخدمة استهلاك بطاقة النادي الرياضي "
                "لرفع سعة حمل العناصر حتى عشر درجات، مع استعادة الدرجات المسجلة عند زوال أثر المهارة. "
                "نُقحت الحوارات والخيارات سياقيا، وأزيلت المخاطبة المذكرة والعبارات العامية، ووُحد مصطلح "
                "زيادة حد الوزن مع قاموس المهارات. حُفظ الاسم الداخلي Ripped Cabus#GymPass والمعرف 7776 "
                "والمتغير gympassmemory والمهارة ALL_INCCARRY وحد 10 والمؤثرات الأربعة وجميع الشروط "
                "وأوامر delitem وskill وclose دون تغيير."
            ),
            "المرحلة_261",
        ]) + "\n"
    write_text_preserve(completed_path, completed, c_bom, c_nl)

    # Remaining list.
    remaining_text, r_bom, r_nl = read_text_preserve(remaining_path)
    remaining_lines = remaining_text.replace("\r\n", "\n").replace("\r", "\n").splitlines()
    original_count = len(remaining_lines)
    remaining_lines = [line for line in remaining_lines if not line.startswith(MAIN_REL + ",")]
    if len(remaining_lines) != original_count - 1:
        raise AssertionError("Failed to remove exactly one gympass row")
    if len(remaining_lines) < 2 or not remaining_lines[1].startswith(NEXT_FILE + ","):
        raise AssertionError("Unexpected next file")
    write_text_preserve(remaining_path, "\n".join(remaining_lines) + "\n", r_bom, r_nl)

    # Changes CSV from line-level replacements.
    old_lines = original_text.splitlines()
    new_lines = final_text.splitlines()
    changes = []
    for i, (old, new) in enumerate(zip(old_lines, new_lines), 1):
        if old != new:
            changes.append([len(changes)+1, MAIN_REL, i, "تنقيح لغوي", old, new, "تغيير نصي فقط؛ منطق السكربت محفوظ"])
    if len(changes) != STAGE_CHANGES:
        raise AssertionError(f"Expected {STAGE_CHANGES} changed lines, got {len(changes)}")
    with (STAGE_DIR / "تقرير_المرحلة_261_التعديلات.csv").open("w", encoding="utf-8-sig", newline="") as f:
        writer = csv.writer(f, lineterminator="\n")
        writer.writerow(["رقم", "الملف", "السطر", "النوع", "النص قبل", "النص بعد", "ملاحظة"])
        writer.writerows(changes)

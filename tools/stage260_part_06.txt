    final_track_csv = track_csv_path.read_text(encoding="utf-8-sig")
    final_completed = completed_csv_path.read_text(encoding="utf-8-sig")
    final_remaining = remaining_csv_path.read_text(encoding="utf-8-sig")
    nav_final = navi_path.read_bytes()

    checks: list[dict[str, str]] = []
    add_check(checks, "وجود الملف الأساسي", main_path.exists(), MAIN_REL)
    add_check(checks, "وجود المرجع الإنجليزي الداخلي", en_path.exists(), EN_REL)
    add_check(checks, "ترميز الملف الأساسي UTF-8", True, "UTF-8")
    add_check(checks, "عدم وجود BOM", not final_main.startswith(b"\xef\xbb\xbf"), "UTF-8 دون BOM")
    add_check(checks, "الحفاظ على LF دون CRLF", b"\r\n" not in final_main, f"{final_main.count(b'chr(10)') if False else final_main.count(bytes([10]))} LF")
    add_check(checks, "حفظ عدد الأسطر", len(final_text.splitlines()) == 56, "56 سطرا")
    add_check(checks, "مطابقة المرجع الرسمي ببصمة Git", git_blob_sha(english) == OFFICIAL_BLOB, OFFICIAL_BLOB)
    add_check(checks, "حفظ المرجع الرسمي في تقرير المرحلة", p(official_rel).read_bytes() == english, "byte-identical")
    add_check(checks, "حفظ النسخة الاحتياطية", p(backup_rel).read_bytes() == original_main, "byte-identical")
    add_check(checks, "تطبيق 18 تعديلا نصيا", len(applied) == STAGE_CHANGES, str(len(applied)))
    add_check(checks, "حفظ خريطة NPC", "pvp_y_room,77,85,4" in final_text, "pvp_y_room")
    add_check(checks, "حفظ الاتجاه", "pvp_y_room,77,85,4" in final_text, "4")
    add_check(checks, "حفظ صورة NPC", "\t105,{" in final_text, "105")
    add_check(checks, "حفظ الاسم الداخلي", "::Guild Battle Guide" in final_text, "Guild Battle Guide")
    add_check(checks, "حفظ الاسم الظاهر", "مرشد معارك النقابات::Guild Battle Guide" in final_text, "مرشد معارك النقابات")
    add_check(checks, "حفظ مصفوفة الفريق الأول", "setarray .@map1X[1],7,9,16,8,20;" in final_text, "مطابق")
    add_check(checks, "حفظ مصفوفة الفريق الثاني", "setarray .@map2X[1],91,90,83,91,79;" in final_text, "مطابق")
    add_check(checks, "حفظ مصفوفة المحور Y", "setarray .@mapY[1],49,49,50,49,50;" in final_text, "مطابق")
    add_check(checks, "حفظ خمسة اختيارات خرائط", 'select("الخريطة 1:الخريطة 2:الخريطة 3:الخريطة 4:الخريطة 5")' in final_text, "5")
    add_check(checks, "حفظ اختيار فريقين", 'select("الفريق 1:الفريق 2")' in final_text, "2")
    add_check(checks, "حفظ خيار نعم/لا", 'select("نعم:لا") == 2' in final_text, "مطابق")
    add_check(checks, "حفظ فرع الإلغاء", 'if(select("نعم:لا") == 2) {' in final_text, "مطابق")
    add_check(checks, "حفظ close", final_text.count("\t\tclose;") == 1, "1")
    add_check(checks, "حفظ close2", final_text.count("\tclose2;") == 1, "1")
    add_check(checks, "حفظ warp", final_text.count('\twarp "guild_vs"') == 1, "1")
    add_check(checks, "حفظ end", final_text.count("\tend;") == 1, "1")
    add_check(checks, "حفظ تعبير اسم خريطة النقل", '"guild_vs"+.@i' in final_text, "مطابق")
    add_check(checks, "حفظ تعبير إحداثي X", 'getd(".@map"+.@j+"X["+.@i+"]")' in final_text, "مطابق")
    add_check(checks, "حفظ تعبير إحداثي Y", ".@mapY[.@i]" in final_text, "مطابق")
    add_check(checks, "حفظ متغير اختيار الخريطة", "set .@i" in final_text, ".@i")
    add_check(checks, "حفظ متغير اختيار الفريق", "set .@j" in final_text, ".@j")
    add_check(checks, "حفظ عدد أوامر mes", final_text.count("\tmes ") == original_text.count("\tmes "), str(final_text.count("\tmes ")))
    add_check(checks, "حفظ عدد أوامر next", final_text.count("\tnext;") == original_text.count("\tnext;"), str(final_text.count("\tnext;")))
    add_check(checks, "حفظ عدد select", final_text.count("select(") == original_text.count("select("), str(final_text.count("select(")))
    add_check(checks, "حفظ الأرقام البرمجية", re.findall(r"\d+", re.sub(r'"[^"]*"', '""', final_text)) == re.findall(r"\d+", re.sub(r'"[^"]*"', '""', original_text)), "مطابق")
    add_check(checks, "حفظ الرموز الداخلية بعد ::", re.findall(r"::([A-Za-z0-9_ #]+)", final_text) == re.findall(r"::([A-Za-z0-9_ #]+)", original_text), "مطابق")
    add_check(checks, "إزالة مخاطبة اللاعب المذكرة", "هل أنت مستعد" not in final_text, "محايد")
    add_check(checks, "تحسين سؤال الاستعداد", "هل نبدأ" in final_text and "الانتقال الآن؟" in final_text, "مطابق")
    add_check(checks, "تحسين رسالة النجاح", "أتمنى لك وقتا ممتعا" in final_text, "مطابق")
